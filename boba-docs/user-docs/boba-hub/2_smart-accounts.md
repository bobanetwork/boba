# Smart Accounts

Smart accounts are accounts that allow you to execute actions in crypto, specifically ones powered by smart contracts (programmable code). They allow more flexibility than the standard Externally Owned Accounts (EOAs), and rely on account abstraction.

## Account Abstraction
